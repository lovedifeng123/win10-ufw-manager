## v2.19.1 修复版（2026-08-23）

### ✅ 修复：v2.19 双击打不开（No module named 'tkinter'）
- 根因：构建用的 Python 虚拟环境缺少 tkinter 模块，导致 exe 启动即报错退出。
- 修复：改用本机完整 Python 3.13 安装（自带 tkinter + tcl/tk）重建构建环境，
  重新打包。已实测 exe 可正常启动并加载 UI（启动进程常驻、内存正常，无 tkinter 报错）。

### 🛡️ 清理功能进一步收窄为纯缓存（防误删，回应资料丢失事故）
- 清理项从 29 项精简为 **8 项**，且**全部为 C: 盘纯缓存目录**：
  用户临时文件、系统 Temp、Windows 更新下载缓存、传递优化缓存、
  Chrome/Edge 浏览器缓存、Prefetch、缩略图缓存。
- **不再触碰**任何用户资料、配置、日志、安装源、驱动残留、升级遗留等风险项。
- 保留 v2.19 的多重护栏：
  - `_abs_base` 校验——环境变量为空直接跳过，绝不退化为裸 `*`；
  - `_safe_to_remove` 二次校验——被删路径绝不可能是盘符根 / 用户主目录；
  - 清理前强制弹「⚠️ 慎用 — 免责声明」对话框，须点「是」才执行。
- 已加深度安全回归测试 `test_cache_safety.py`：真实只读扫描断言清理**仅作用于 C: 缓存目录**，
  绝不包含 D: / E: / 用户主目录。实测本机仅涉及 6 个 C: 缓存目录、0 个数据盘文件。

### ⚙️ 其它
- 版本串统一为 v2.19.1（主窗口标题/托盘一致）。
- `on_toggle` 仅管理 C: 盘，D:/E: 等数据盘绝不纳入 UWF 保护（避免软件只写覆盖层、重启丢失）。

### 📌 版本与发布说明
- 本发布包为修复版 **v2.19.1** 可执行文件（已部署于桌面 `UWF Manager Pro v2.19.1.exe`）。
- 源码已本地提交 v2.19.1（版本串对齐 + 清理收窄 + 安全测试）；因当前沙箱网络限制
  暂无法推送到 GitHub，下个可联网环境将补全推送。

### 历史版本
- v2.18 / v2.17 / v2.16 因清理误删 bug 已下架。
- 早期可用版本：v2.7、v2.4、v2.3。
